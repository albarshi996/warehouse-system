# بطاقة الاستئناف — مواءمة SAP — خطة٤

> **ملفّ مولَّد آليًّا — لا يُحرَّر بيد.** مصدره [`sap-alignment.json`](sap-alignment.json)؛ يُعاد توليده بـ`npm run plan:sap`.
> **اقرأني أوّلًا في كلّ جلسة جديدة.** لا تقرأ `SESSION_HANDOFF.md` كلّه — هذه الصفحة تكفي للاستئناف.

**الخطة:** [مواءمة البوابة مع منطق المستندات المترابطة (خطة٤)](../) · **المسار:** جراحيّ · حزمة واحدة في المرّة · لا كودَ قبل اكتمال الجرد (§6 ‹120›)
**آخر تحديث:** 2026-08-12 بواسطة Claude
**التقدّم:** 2 من 19 مهمّة (11٪)

## أين توقّفنا
### SAP-16 — مرآة أودو الماليّة — استيراد شجرة الحسابات والقيود والفواتير والمدفوعات  `حم`

**ما أُنجز منها:** **مبنيّةٌ ومدفوعة بالكامل** (`a25f88f` العقد · `0acd79f` الخدمة والواجهة · `2c563b9` تشخيص الفشل). سجلّ خمسة نطاقات بمعرّف مرآةٍ حتميّ · مطابقاتٌ مالية (الفاتورة `account.move` بنوعٍ مختلف · الرصيد المفتوح مستوردٌ لا محسوب · التوازن يُعلَن ولا يُسوّى) · خدمة سحبٍ عامّة (ما يختفي يُوسم `missingInOdoo` ولا يُحذف · نطاقٌ يفشل لا يحجب الباقي) · بذرة مالية في المحاكي (10 حسابات · 4 قيود · 6 أسطر · دفعتان) · زرّ «اسحب من أودو» في كلّ التبويبات + دوريّة ٥ دقائق قابلة للإيقاف + «آخر سحب» محسوبًا من أقدم نطاق · 5 مجموعات في القواعد (لا حذف أبدًا) والملفّ 1193 سطرًا على main. **25 اختبارًا. البوّابات: 1355/1355 · build 96 · audit نظيف · lint 126 بلا زيادة.**

**الخطوة التالية حرفيًّا:** **سؤالٌ واحد مفتوح يُغلق المهمّة:** لقطة المالك 2026-08-13 بعد النشر أظهرت «شجرة الحسابات 10 · الآن» (⇒ القواعد والسحب والمرآة والحالة كلّها تعمل) لكنّ النطاقات الثلاثة الباقية «لم يُسحب». ثلاثة تفسيرات: (١) اللقطة أثناء السحب — النطاقات تُسحب تتابعًا والحسابات أوّلها؛ (٢) المنشور يحوي `odoo_accounts` و`odoo_pull_state` دون الأربعة الباقية؛ (٣) عطبٌ في الكتابة. **الحسم:** اطلب من المالك ضغط «اسحب من أودو» ولقطةً — الشارات صارت تُظهر «تعذّر» بالأحمر مع السبب في التلميح (`2c563b9` أضاف حفظ النتيجة في الذاكرة، فالفشل الذي تعجز القاعدة عن حفظه يبقى مرئيًّا). وقد ثبت آليًّا أنّ المحاكي يُخرج 10/4/6/2 سجلًّا بمعرّفات صالحة — فالقراءة والمطابقة سليمتان قطعًا، والعطب إن وُجد في الكتابة وحدها.

**الفرع:** `main`

**الملفّات المعنيّة:** `src/services/odoo/pullRegistry.js` · `src/services/odoo/financeMapper.js` · `src/services/odoo/pullService.js` · `src/services/odoo/mockOdooClient.js` · `src/components/brandzo-erp/odoo-sync/OdooSyncBridge.jsx` · `firestore.rules`

**الاختبار الحارس المطلوب:** `src/services/odoo/pullContract.test.js`

**معيار الإتمام:**
- [ ] استيراد شجرة الحسابات بمفاهيمها الإحدى عشر — §16.4 ‹504-514› — بلا حسابٍ أو رقمٍ مخترع (§16.4 ‹549›)
- [ ] استيراد القيود بحقولها السبعة ومعرّف idempotency — §16.9 ‹652-658›
- [ ] استيراد المستندات الماليّة الموجودة فعلًا في أودو، لا القائمة الاسميّة كلّها — §16.6 ‹597›
- [ ] كلّ سجلٍّ مستورد يحمل: هويّة الربط · الحالة · وقت المزامنة · النتيجة — §16.5 ‹500›
- [ ] المرآة ملحقة-فقط أو مستبدَلة بالكامل عند السحب — لا تحرير محلّيّ لسجلٍّ ماليّ
- [ ] فشل السحب يظهر حالةً غير مكتملة لا ترحيلًا ناجحًا — §16.21-١٩ ‹827›
- [ ] بيانات اعتماد أودو تبقى في الوكيل ولا تصل إلى المتصفح — §16.20 ‹801›
- [ ] لا تظهر تفاصيل الحسابات أو القيود لمستخدمٍ غير مخوّل — §16.20 ‹803›

## الجاهز للبدء الآن (بلا انتظار أحد)
- **SAP-1** — هويّة الصنف وبطاقته — الكود هو الهويّة، والباركود وسيلة بحث `ح1`
- **SAP-6** — الخريطة — مفتاح «إظهار المراجع» وتمييز العقدة المرجعيّة `ح5`
- **SAP-11** — المرفقات — النسخة والبصمة والمواضع التسعة `ح8`

## المحجوب
- **SAP-4** — حالات المستند — closed وcanceled مخزَّنتان، والباقي محسوب — _محجوب بـ:_ قرار‑٣ — موافقة المالك على نموذج الحالتين المخزَّنتين مقابل الثلاث المحسوبة
- **SAP-8** — التتبّع التسلسليّ Serial — _محجوب بـ:_ قرار‑٤ — موافقة المالك على تمثيل التسلسليّ كدفعةٍ كمّيّتها ١

## على المالك
- **قرار‑٨** — مصير خطة الانتقال إلى Supabase (docs/plan/مراجعة-خطة-الانتقال-إلى-Supabase.md): تأجيلها إلى ما بعد إغلاق SAP-0، واعتماد قاعدة «لا استيراد Firestore مباشرةً في ملفٍّ جديد» فورًا. ثلاث جبهاتٍ جراحيّة (نسخة العميل · مواءمة خطة٤ · الانتقال) لا تجتمع على كودٍ واحد _(إن لم يتوفّر: خطر كتابة كلّ مهمّة SAP مرّتين — مرّةً على Firestore ومرّةً على Postgres — أو رمي إحداهما؛ ويخالف §22 ‹994› و§5 ‹102›)_
- **قرار‑٧** — إقرار أثر ختم الدفع: جسر المزامنة الحيّ القائم (PO والماستر درفتًا في أودو حتى الاعتماد) سيتوقّف، وتصير شاشة /dashboard/odoo-sync استيرادًا لا دفعًا. الكود لا يُحذف بل يُختم — قابلٌ للفكّ بقرارك (§3-٣ ‹42› و§4 ‹79› يمنعان الحذف) _(إن لم يتوفّر: لا يُختم الدفع، ويبقى مصدران محتملان لرقمٍ واحد)_
- **قرار‑٣** — الموافقة على إضافة حالتَي closed وcanceled المخزَّنتين، وإبقاء open/partial/fulfilled محسوبةً من الرصيد المفتوح لا مخزَّنة _(إن لم يتوفّر: تبقى ف‑١٤ مفتوحة، وتتعطّل §12 و§13 و§18 جزئيًّا)_
- **قرار‑٤** — الموافقة على تمثيل الرقم التسلسليّ كدفعةٍ كمّيّتها ١ (SN:‹الرقم›) بدل تغيير مفتاح الرصيد _(إن لم يتوفّر: تبقى ف‑٤ مفتوحة، ويتعذّر سيناريو §21-١١)_
- **قرار‑٥** — نشر firestore.rules بعد إضافة مجموعة item_partner_catalog (§3-١٠ ‹49› يمنعني من نشرها) _(إن لم يتوفّر: كلّ تحقّقٍ حيّ على الكتالوج يفشل، ويبقى الإثبات آليًّا لا حيًّا)_
- **قرار‑٦** — هل فروعنا مستودعاتٌ داخل كيانٍ واحد أم كيانات قانونيّة مستقلّة؟ (§16.13 ‹721› يأمرني بالتوقّف هنا) _(إن لم يتوفّر: لا يجوز افتراض أيّ من الاثنين؛ يتوقّف تصميم النقل الداخليّ الماليّ)_

## القاعدة عند كلّ إغلاق مهمّة
١. حدّث حالة المهمّة و`evidence.commit` في `sap-alignment.json`.
٢. `npm run plan:sap` — يُعيد توليد هذه البطاقة ويرفض الإغلاق بلا بيّنة.
٣. سطر في `SESSION_HANDOFF.md` بمعرّف المهمّة.

---
_مولَّد بـ`scripts/plan-check.mjs`_
