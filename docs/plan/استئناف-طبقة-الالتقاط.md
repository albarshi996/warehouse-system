# بطاقة الاستئناف — طبقة الالتقاط (الجرد)

> **ملفّ مولَّد آليًّا — لا يُحرَّر بيد.** مصدره [`capture-layer.json`](capture-layer.json)؛ يُعاد توليده بـ`npm run plan:capture`.
> **اقرأني أوّلًا في كلّ جلسة جديدة.** لا تقرأ `SESSION_HANDOFF.md` كلّه — هذه الصفحة تكفي للاستئناف.

**الخطة:** [طبقة الالتقاط — صفحة الجرد تلتقط ولا تُحاسِب](../خطة-طبقة-الالتقاط.md) · **المسار:** الالتقاط أوّلًا · المطابقة مؤجَّلة بقرار المالك (ق-٦ «تأجيل» 2026-08-23) · ولا مهمّة تحجب العادّ على الرفّ
**آخر تحديث:** 2026-08-23 (CAP-000 مغلقة · CAP-101 مفتوحة) بواسطة Claude
**التقدّم:** 1 من 29 مهمّة (3٪)

## أين توقّفنا
### CAP-101 — شاشة العدّ تتجرّد من الدفتريّ والفرق  `م١`

**ما أُنجز منها:** لم يبدأ التنفيذ بعد — فُتحت المهمّة مباشرةً بعد إغلاق CAP-000.

**الخطوة التالية حرفيًّا:** قراءة جدول الجلسة في ScanFlow.jsx (الأسطر 840-930) وbuildSessionRows/filterRows/exportRows في scanFlow.js، ثمّ نزع bookQty وdiff من النواة أوّلًا وإسقاط عموديهما وتبويب الفروقات من الشاشة، ثمّ اختبارٌ يثبت أنّ الصفّ لا يحمل رصيدًا.

**الفرع:** `claude/warehouse-inventory-architecture-d68500`

**الملفّات المعنيّة:** `src/components/brandzo-erp/stock/ScanFlow.jsx` · `src/services/stock/scanFlow.js`

**الاختبار الحارس المطلوب:** `src/services/stock/scanFlow.test.js`

**معيار الإتمام:**
- [ ] عمودا «الدفتريّ» و«الفرق» يختفيان من جدول الجلسة، وتبويب «الفروقات» معهما
- [ ] أعمدة الالتقاط وحدها تبقى: الصنف · المعدود · الوحدة · بواسطة · الوقت · الحالة
- [ ] buildSessionRows لا يحسب فرقًا ولا يقرأ item.balance إطلاقًا — الحساب ينتقل كاملًا لطبقة المطابقة
- [ ] قاعدة الجرد من الماستر تبقى: «ما لم يُعدّ بعد» عملٌ متبقٍّ لا فرق، وهو جوهر الجرد
- [ ] exportRows يصدّر ما التُقط فقط — ولا يدّعي رصيدًا
- [ ] اختبارٌ يثبت أنّ صفًّا لصنفٍ برصيدٍ ٤٧٥ لا يحمل أيّ حقل رصيدٍ ولا فرق

## الجاهز للبدء الآن (بلا انتظار أحد)
- **CAP-102** — الوحدة تُحلّ من الباركود لا تُكتب — وصل جدول الباركودات المهجور `م١`

## المحجوب
_لا شيء محجوب._

## على المالك
- **CAP-305** — تجربة قطع شبكة حيّة بجهازين على العمليّة نفسها
- **CAP-O01** — تجربة قطع شبكةٍ حيّة بجهازين على العمليّة نفسها — لا تُثبَت الحتميّة والطابور باختبار وحدة _(إن لم يتوفّر: يبقى م٣ مبنيًّا غير مجرَّب، ويُوسم كذلك في بطاقة الاستئناف بدل أن يُعلن جاهزًا)_
- **CAP-O02** — تأكيد نصّ سبب الإلغاء الذي يُختم على الـ١٣ عمليّة المفتوحة (ق-١) — يُكتب مرّةً ويبقى في السجلّ للأبد _(إن لم يتوفّر: يُكتب: ألغيت بقرار المالك 2026-08-23 — بلا نطاق ولا ختم، لا تُحسب جردًا)_

## القاعدة عند كلّ إغلاق مهمّة
١. حدّث حالة المهمّة و`evidence.commit` في `capture-layer.json`.
٢. `npm run plan:capture` — يُعيد توليد هذه البطاقة ويرفض الإغلاق بلا بيّنة.
٣. سطر في `SESSION_HANDOFF.md` بمعرّف المهمّة.

---
_مولَّد بـ`scripts/plan-check.mjs`_
