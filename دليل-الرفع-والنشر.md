# دليل الرفع والنشر — الإجراء المعتمد (SOP)

> **ما هذا؟** الإجراء الكامل الذي تمرّ به أي شفرة قبل أن تصل إلى المستخدم في براندزو: من فتح الفرع، مرورًا بالبوابات الأربع وقائمتَي الهوية البصرية وحماية الكود، حتى النشر على الموقع الحيّ والتحقّق بعده.
>
> **لمن؟** لكل شريك تنفيذي جديد في الفريق (Codex · Claude · أي مطوّر بشري). اقرأه مرّة كاملًا، ثم اعمل ببطاقة §10 المرجعية.
>
> **الحاكم:** ما في هذا الملف تفصيلٌ لقواعد [`AGENTS.md`](../AGENTS.md) و[`docs/إحاطة-كوديكس.md`](إحاطة-كوديكس.md) — عند أي تعارض فالنصّ الأقصر في `AGENTS.md` هو الحاكم.
>
> **آخر تحديث:** 2026-08-07 · **أرقام مرجعية مقيسة اليوم فعليًّا على هذا الفرع** (لا منقولة عن وثيقة): 728 اختبارًا خضراء · بناء 64 صفحة · تدقيق نظيف (44 قاعدة أمان · 14 دورًا) · خط أساس اللينت 126 مشكلة.

---

## 0. القاعدة الصفرية

**المالك وحده يقول «ارفع».** لا دفع إلى `main` بمبادرة من وكيل أو مطوّر مهما اكتملت الجودة. البوابات كلها في هذا الدليل تُجهّز العمل للحظة هذا الأمر — ولا تُغني عنه.

والقاعدة الثانية بعدها: **صفر حذف.** لا تُحذف ميزة ولا صفحة ولا يُغيَّر رابط قائم؛ يُنقل ويُربط.

---

## 1. الصورة الكبرى — رحلة السطر الواحد

```
   محرّرك                                                          المستخدم
     │                                                                 ▲
     │  1. طقس البداية        2. فرع معزول        3. البوابة الخضراء   │
     ├─── git pull ──────────► claude/* أو codex/* ──► build·test·audit·lint
     │    قراءة SESSION_HANDOFF                              │
     │                                                       ▼
     │                                        4. بوابة الهوية البصرية (§5)
     │                                        5. بوابة حماية الكود (§6)
     │                                                       │
     │                                                       ▼
     │                                              6. كومِت عربي موقّع
     │                                                       │
     │                                    ┌──────────────────┴─────────────┐
     │                                    │  7. إذن المالك: «ارفع»          │
     │                                    └──────────────────┬─────────────┘
     │                                                       ▼
     │                                     git push origin HEAD:main
     │                                                       │
     │                       ┌───────────────────────────────┴───────────────┐
     │                       │  GitHub Actions — astro.yml (ثلاث وظائف)      │
     │                       │  Quality Gate ──► Build ──► Deploy             │
     │                       │  اختبارات+تدقيق   بناء Astro   نشر Pages       │
     │                       └───────────────────────────────┬───────────────┘
     │                                                       ▼
     │                          https://albarshi996.github.io/warehouse-system/
     │                                                       │
     └──── 9. تحديث SESSION_HANDOFF ◄──── 8. تحقّق حيّ (كاش 10 دقائق) ◄────┘
```

**نقطتان لا تُنشران آليًّا** (تفصيلهما §7): قواعد الأمن `firestore.rules` — ينشرها المالك يدويًّا من Firebase Console · وبرنامج ويندوز — خطّ نشر منفصل.

---

## 2. من يملك ماذا — جدول الصلاحيات

| القرار / الفعل | المالك (محمد البرشي) | الشريك التنفيذي (Codex · Claude) |
|---|---|---|
| الدفع إلى `main` | **يأمر به** | ينفّذ بعد الأمر فقط |
| نشر `firestore.rules` على Firebase | **هو وحده** (Console) | يعدّل الملف في الريبو ويطلب النشر |
| حذف أي شيء (صفحة · ميزة · حقل · سجل) | **هو وحده** | ممنوع مطلقًا |
| تغيير أو إزالة رابط (URL) قائم | **هو وحده** | ممنوع — الإضافة والربط فقط |
| الهوية البصرية (اللون · الخط · الشعار) | **هو وحده** | يلتزم بالتوكِنز القائمة |
| أسرار GitHub / Firebase / أودو | **هو وحده** | لا يراها ولا يطبعها ولا يكتبها |
| ربط النطاق `warehousehub.com.ly` | **هو وحده** (مسجّل Libyan Spider) | يجهّز `CNAME` و`base` عند الطلب |
| ترقية Firebase لخطة Blaze | **هو وحده** | يخطّط لما بعدها |
| إسناد الأدوار للمستخدمين | **هو وحده** | يعرّف الدور في الكود لا في البيانات |
| فتح فرع والعمل فيه | يوجّه | **يملكه** |
| الاختبارات والتدقيق والبناء | يطّلع على النتيجة | **يملكها ويضمنها** |

**قاعدة التصادم:** مهمة واحدة → وكيل واحد. إن وجدت في `SESSION_HANDOFF.md` مدخلًا حديثًا لوكيل آخر عن عمل غير مرفوع — **لا تلمس نطاقه** واسأل المالك.

---

## 3. الطبقة 1 — طقس ما قبل العمل (ثلاث دقائق، غير قابلة للاختصار)

```bash
git pull
```

1. **`git pull` أولًا دائمًا** — لا عمل على نسخة قديمة. من داخل شجرة عمل (worktree) زامن الفرع الذي تعمل عليه.
2. **اقرأ رأس [`SESSION_HANDOFF.md`](../SESSION_HANDOFF.md)** — سطر «آخر تحديث» وقسم «أين توقّفنا». هذه ذاكرة الفريق الحيّة، وفيها ما لم يُرفع بعد.
3. **اقرأ [`AGENTS.md`](../AGENTS.md)** (Codex يقرؤه آليًّا كل جلسة).
4. عند العمل في نطاق له خطة حاكمة في `docs/` — اقرأ الخطة قبل الكود.
5. **تحقّق قبل الثقة:** الوثيقة قد تتقادم؛ الحقيقة في `git log` والكود. لا تُصدر حكمًا على حالة النظام من وثيقة وحدها.

---

## 4. الطبقة 2 — الفرع والعزل

| القاعدة | التفصيل |
|---|---|
| بادئة الفرع | `codex/<وصف>` لكوديكس · `claude/<وصف>` لكلود · `feat/<وصف>` لمطوّر بشري |
| لا عمل مباشر على `main` | `main` هو الحيّ: كل دفعة إليه تنشر للمستخدمين خلال دقائق |
| شجرة عمل معزولة (worktree) | مسموحة ومفضّلة للمهام المتوازية — لكن الدفع منها بصيغة `HEAD:main` (لا يمكن `checkout main` وهي محجوزة) |
| فرع واحد لمهمة واحدة | لا تخلط مهمّتين في فرع؛ يصعّب التراجع |
| لا `force push` ولا إعادة كتابة تاريخ | التاريخ سجلّ رسمي — التصحيح بكومِت جديد أو `revert` |

---

## 5. الطبقة 3 — البوابة الخضراء (أربعة أوامر، إلزامية قبل كل كومِت نهائي)

```bash
npm test && npm run audit && npm run build
```

| الأمر | ماذا يفحص بالضبط | معيار النجاح (قياس 2026-08-07) | عند الفشل |
|---|---|---|---|
| `npm test` | `node --test` على `src/**/*.test.js` — المنطق الخالص: الترقيم · آلات الحالات · FEFO · الوصول والأدوار · الإكسل · الهيكل… | **728/728 خضراء · صفر فشل** | لا تُطفئ اختبارًا ولا تعدّل توقّعه لتمرير الشفرة. أصلح الشفرة أو ناقش الخطأ مع المالك |
| `npm run audit` | حارس البنية: (1) صفحة يتيمة لا تصلها القائمة (2) رابط مكسور (3) **مجموعة Firestore بلا قاعدة أمان** (4) صفحة حسّاسة يفتحها دور لا يخصّه | «التدقيق نظيف» — 62 صفحة مربوطة · 58 رابطًا داخليًّا · 44 قاعدة · 28 صفحة حسّاسة · 14 دورًا | الرسالة تقول أي بند سقط. الأشيع: نسيت إضافة الصفحة إلى `navCatalog` أو نسيت قاعدة أمان للمجموعة الجديدة |
| `npm run build` | بناء الإنتاج + `prebuild` تلقائيًّا (`build-arch` + `build-org`) + توليد `sw.js` للأوفلاين | **64 صفحة** + سطر `sw.js مكتوب` | خطأ البناء يمنع النشر أصلًا في CI — أصلحه محليًّا لا في السحابة |
| `npx eslint .` | جودة الشفرة | **خط الأساس أحمر أصلًا: 126 مشكلة (53 خطأ + 73 تحذيرًا)** — يُحكم على **الجديد فقط** | قِس قبل تغييرك وبعده وقارن؛ لا تحاول تصفير الدَّين القديم في مهمّة غير مخصّصة له. لقياس شفرتك وحدها: `npx eslint src` |

**قاعدة القراءة:** `npm test` و`npm run audit` **حاجزان صلبان** — يُشغَّلان مرّة أخرى في السحابة، وفشل أحدهما يوقف البناء والنشر كليًّا. اللينت يُطبع ولا يحجب.

**زيادة عند تعديل قواعد الأمن فقط:**

```bash
npm run test:rules
```

يشغّل اختبارات `test/firestore-rules.test.mjs` على مُحاكي Firestore (يتطلّب Firebase CLI). **أي تعديل على `firestore.rules` لا يُطلب نشره من المالك قبل أن يمرّ هذا.**

---

## 6. الطبقة 4 — بوابة الهوية البصرية (قبل الكومِت)

الهوية عندنا قرار مالك لا ذوق مطوّر. هذه اثنتا عشرة نقطة تُراجَع بالعين والشفرة معًا:

| # | القاعدة | كيف تتحقّق عمليًّا |
|---|---|---|
| 1 | **بلا إيموجي في الواجهات** — أيقونات SVG خطية فقط | ابحث في ملفاتك المعدّلة عن أي رمز تعبيري داخل نصّ يراه المستخدم. (الوثائق والكومِتات مستثناة، والواجهة لا) |
| 2 | **الأحمر للتحذير فقط** | الأحمر يعني «انتبه/خطأ/تجاوز». لا يُستخدم زخرفةً ولا لعنوان ولا لزرّ عادي |
| 3 | **اللون من نقطة تحكّم واحدة** | `--o-brand-primary` في [`src/styles/tokens.css`](../src/styles/tokens.css) = `#714B67` (أودو المؤسسي، قرار المالك 2026-07-30). **يُمنع كتابة لون هوية داخل مكوّن** |
| 4 | **الثيم `bz-light`** | الشاشات الجديدة تُبنى بمكوّنات `src/components/odoo/` (ListView · FormSheet · Badge…) لا بأنماط مرتجلة |
| 5 | **الخط IBM Plex Sans Arabic مستضاف ذاتيًّا** | من `src/styles/fonts.css` — **ممنوع Google Fonts أو أي CDN** |
| 6 | **بنية اللوحات ثلاث طبقات** | تدخّل الآن ← إجراءات سريعة ← فهرس كامل. أي لوحة جديدة تتبع الترتيب نفسه |
| 7 | **عربية RTL كاملة + أرقام لاتينية** | كل ما يراه المستخدم عربي؛ الأرقام `1234` لا `١٢٣٤` داخل الواجهة |
| 8 | **لا حذف ولا تغيير لأي URL قائم** | الرابط المنشور عقد مع المستخدم. الإضافة نعم، التغيير لا |
| 9 | **`navCatalog` مصدر التنقّل الوحيد** | [`src/services/auth/navCatalog.js`](../src/services/auth/navCatalog.js) — منه تُرسم القائمة وتُشتقّ الصلاحيات ويقرأ التدقيق. لا قائمة ثانية يدوية أبدًا |
| 10 | **الربط قبل الإضافة** | صفحة لا تصلها القائمة = صفحة يتيمة يرفضها `npm run audit` |
| 11 | **زرّ الرجوع يغلق أي طبقة تملأ الشاشة** | عبر [`src/services/ui/overlayHistory.js`](../src/services/ui/overlayHistory.js): `trackOverlay` سطرٌ واحد للطبقات القائمة · `useOverlayBack` لـReact |
| 12 | **التحقّق البصري على البناء لا على dev** | `npm run preview` ثم افحص — `astro dev` فيه خلل ترطيب جزر React |

**الفحص البصري الذي لا يُختصر:** افتح الشاشة فعلًا (سطح مكتب + هاتف)، وتأكّد أن لا شيء انكسر في الشاشات المجاورة.

---

## 7. الطبقة 5 — بوابة حماية الكود والبيانات (قبل الكومِت)

هذه أخطر بوابة: خطأ الهوية يُرى ويُصلَح، وخطأ الأمن قد لا يُرى حتى يقع الضرر.

### أ. الأسرار — القاعدة الحديدية

| النوع | أين يعيش | أين لا يعيش أبدًا |
|---|---|---|
| مفاتيح Firebase الستة `PUBLIC_FIREBASE_*` | `.env` محليًّا (متجاهَل في git) + **GitHub Secrets** للنشر | لا داخل الشفرة ولا في كومِت |
| أسرار أودو الحقيقية (`ODOO_URL` · `ODOO_DB` · `ODOO_USER` · `ODOO_API_KEY`) | **خزنة الوسيط الخادمي فقط** (`odoo-proxy`) | **ممنوع منعًا باتًّا** أن تحمل بادئة `PUBLIC_` — البادئة تعني «يُحقن في حزمة المتصفّح» أي ينكشف للعالم |
| كلمات مرور المستخدمين | Firebase Auth | لا في Firestore ولا في سجلّ ولا في مستند |

قاعدة عملية: **بادئة `PUBLIC_` = علنيّ.** إن كان السرّ سرًّا حقًّا فلا يستحقّ هذه البادئة. مفاتيح Firebase الويب مصمّمة لتُشحن للمتصفّح ويحميها `firestore.rules` + Auth، لا إخفاؤها. راجع [`.env.example`](../.env.example).

### ب. قواعد الأمن — مسار ذو خطوتين

1. **الوكيل يعدّل** [`firestore.rules`](../firestore.rules) في الريبو ويشغّل `npm run test:rules`.
2. **المالك وحده ينشرها**: Firebase Console ← Firestore Database ← Rules ← لصق الملف ← Publish.

> **درس مكلف (2026-08-07):** كلمة «نشرتُ» لا تُصدَّق بالثقة بل **بالمطابقة الحرفية**: انسخ نصّ القواعد المنشور من Console وطابقه بالملف. في آخر مطابقة تبيّن غياب `odoo_sync` و`odoo_sync_events` من المنشور رغم وجودهما في الريبو. **مجموعة بلا قاعدة منشورة = كتابة ترتدّ `permission-denied` في وجه الموظّف.**

### ج. السجلّات المقدّسة (ملحقة-فقط)

`audit` · `scans` · `events` · `stock_moves` · `assignments` · `control` · `attachments`

**لا تعديل ولا حذف على سجلاتها أبدًا** — التصحيح بقيد جديد يشرح، لا بمحو القديم. هذا مفروض في القواعد على الخادم، ومن يحاول التحايل عليه من الواجهة يكسر حجّية النظام كلّه.

### د. الأدوار والصلاحيات

- الأدوار الأربعة عشر في [`src/services/auth/roles.js`](../src/services/auth/roles.js)؛ الدور المجهول يسقط على `viewer` (أقلّ صلاحية) **لا** على «بلا قيد».
- الحارس ثلاث طبقات: القائمة (`navCatalog`) ← الشاشة (`pageAccess` + `AuthGate`) ← **الخادم (`firestore.rules`)**. الأخيرة وحدها هي الأمن الحقيقي؛ الأوليان راحة استخدام.
- أي صفحة حسّاسة جديدة: أضِف `roles` لها في الكتالوج، و`npm run audit` سيثبت أنّ لا دور آخر يفتحها.

### هـ. البيانات والملفات

- **خطة Spark المجانية**: لا Cloud Functions ولا Storage. المرفقات base64 داخل Firestore بحدّ **~700 ك.ب بعد الضغط** — تجاوزها يكسر الكتابة.
- **لا CDN إطلاقًا** — كل أصل مستضاف ذاتيًّا (قاعدة الأوفلاين 100%). إدخال CDN يكسر عمل النظام في مستودع بلا إنترنت.
- **بيانات حقيقية:** لا تضع أسماء موظّفين أو أرقام هواتف أو صور مستندات فعلية داخل ملفات الريبو (بذور الاختبار مثال).
- **التوقيعان الرسميان** (`public/identity/signatures/`) يُدرجان تلقائيًّا في المحاضر والخطابات — **لا يُلفّق توقيع** ولا يُغيَّر مسارهما.

### و. نظافة الكومِت — افحص قبل أن تُضيف

```bash
git status
git diff --stat
```

قبل `git add -A` تأكّد أنّه **لا** يُدرَج شيء من هذه: `.env` · `dist/` · `node_modules/` · `.astro/` · `.claude/settings.local.json` · أي `*.log` · أي ملف ضخم أو لقطة شاشة عابرة. (كلها في `.gitignore` — لكن العين قبل الأداة.)

### ز. الأتمتة الخطرة

في الريبو خطّ أتمتة `gemini-developer.yml` **يستيقظ عند وسم أي Issue بالوسم `ai-develop`** وله صلاحية كتابة على المحتوى وفتح طلبات دمج. لا يُستخدم هذا الوسم إلا بقرار المالك — الوسم ليس تعليقًا، بل تصريح تنفيذ.

---

## 8. الطبقة 6 — الكومِت

**الصيغة:** بادئة إنجليزية + نطاق + وصف عربي.

```
feat(نطاق): وصف عربي موجز لما أُنجز
```

البادئات: `feat` (ميزة) · `fix` (إصلاح) · `docs` (وثائق) · `refactor` (إعادة هيكلة) · `chore` (صيانة).

**أمثلة من تاريخ المشروع:**

```
fix(inventory): جاهزية جرد السبت — استئناف العملية ذريًّا + حفظ الدفعة
feat(portal): مركز متطلبات التكامل BFP-SCM-REQ-2026 — أولى مهام كوديكس [Codex]
```

**قواعد:**
- كومِت واحد = فكرة واحدة مكتملة وخضراء (لا تكوّم عملًا نصفه مكسور).
- ذيّل الكومِت بسطر هويتك (`Co-Authored-By: …`) — الوسم `[Codex]` أو `[Claude]` في العنوان أو في `SESSION_HANDOFF`.
- **ممنوع تخطّي الخطّافات** (`--no-verify`) وتعطيل التوقيع.
- الرسالة تصف **ماذا ولماذا**، لا «تحديثات».

---

## 9. الطبقة 7 — الدفع، ثم المراقبة، ثم التحقّق الحيّ

### أ. الدفع (بعد «ارفع» فقط)

```bash
git push origin HEAD:main
```

`HEAD:main` هي الصيغة الصحيحة من شجرة عمل معزولة (لا يمكن `checkout main` وهي محجوزة لشجرة أخرى). ولو كنت في نسخة عادية على فرعك، فالصيغة نفسها تعمل.

### ب. ما يجري في السحابة — `.github/workflows/astro.yml`

| الوظيفة | ما تفعله | إن فشلت |
|---|---|---|
| **1. Quality Gate** | Node 22 · `npm ci` · `npm test` · `npm run audit` · لينت للعرض فقط (`continue-on-error`) | **لا بناء ولا نشر** — يتوقّف كل شيء |
| **2. Build** | `npm ci` · توليد `build-arch` و`build-org` · `astro build` بحقن `PUBLIC_FIREBASE_*` من GitHub Secrets وبـ`--site`/`--base` من إعداد Pages · رفع الأثر | لا نشر |
| **3. Deploy** | `actions/deploy-pages` إلى بيئة `github-pages` | الموقع يبقى على آخر نشرة ناجحة |

الوظائف متسلسلة (`needs`)، والتزامن مضبوط بمجموعة `pages` بلا إلغاء للجارية. **راقب النتيجة على صفحة Actions قبل أن تعلن الإنجاز** — الإعلان قبل الاخضرار مخالفة لقاعدة «تحقّق قبل الثقة».

### ج. التحقّق الحيّ (بعد اخضرار النشر)

الرابط: `https://albarshi996.github.io/warehouse-system/`

1. **كاش Pages ~10 دقائق على HTML** — إن لم تظهر صفحتك فاعمل `Ctrl+Shift+R` (تحديث قسري) قبل أن تشكّ في النشر.
2. **PWA:** العامل الخفي `sw.js` يُحدَّث ذاتيًّا؛ في الهاتف قد تحتاج إغلاق التطبيق وفتحه.
3. **افتح الشاشة المعنيّة فعلًا** وجرّب المسار كاملًا (لا مجرد فتح الصفحة).
4. **جرّب بدور غير الأدمن** إن كانت الميزة مقيّدة بدور — الأدمن يرى كل شيء فلا يكشف تسريبًا.
5. **افحص كونسول المتصفّح**: صفر أخطاء. أي `permission-denied` يعني قاعدة أمان غير منشورة (§7-ب).

### د. إغلاق المهمة

1. **حدّث رأس [`SESSION_HANDOFF.md`](../SESSION_HANDOFF.md)** بمدخل موسوم `[Codex]` أو `[Claude]` + التاريخ + المنجَز + المتبقّي + أي بند معلّق على المالك (نشر قواعد مثلًا).
2. أبلغ المالك بنتيجة النشر ورابط الصفحة، وبما بقي معلّقًا عليه صراحةً.

---

## 10. ما لا ينشره خطّ الأنابيب — مهام المالك اليدوية

| البند | الإجراء | من |
|---|---|---|
| **قواعد `firestore.rules`** | Console ← Firestore ← Rules ← لصق ← Publish ← **مطابقة حرفية للتأكيد** | المالك |
| **GitHub Secrets** (`PUBLIC_FIREBASE_*` الستة) | Settings ← Secrets and variables ← Actions | المالك |
| **أيقونات PWA** بعد تغيير الهوية | `node scripts/generate-icons.mjs` ثم كومِت | الوكيل بأمر المالك |
| **النطاق `warehousehub.com.ly`** | سجلات A على مسجّل Libyan Spider (185.199.108–111.153) + ملف `CNAME` وضبط `base` في الريبو | المالك عند المسجّل · الوكيل في الريبو |
| **ترقية Blaze** | بطاقة المالك — تفتح Cloud Functions والنسخ المجدول وStorage | المالك |
| **إسناد الأدوار للمستخدمين** | من شاشة الإدارة/Console | المالك |

---

## 11. النشر الثاني — برنامج ويندوز (اختياري، منفصل)

`.github/workflows/desktop-installer.yml` يبني `Brandzo Hub Setup.exe` عبر Tauri **فقط** عند تغيير `src-tauri/**` أو بتشغيل يدوي، ويصدره في صفحة Releases بوسم `desktop-v__VERSION__`.

**اعلم:** البرنامج قشرة حول النظام السحابي الحيّ — تحديث المحتوى يصل عبر الويب تلقائيًّا، فلا تُعِد بناءه إلا إن تغيّرت القشرة نفسها.

---

## 12. التراجع والطوارئ

| الحالة | الإجراء الصحيح | الممنوع |
|---|---|---|
| نشرة كسرت شيئًا في الحيّ | `git revert <hash>` ثم `git push origin HEAD:main` — النشر يعيد الحالة السابقة تلقائيًّا | `git push --force` · حذف كومِت · إعادة كتابة التاريخ |
| فشلت Quality Gate | أصلح محليًّا وادفع كومِتًا جديدًا؛ الموقع الحيّ لم يتأثّر أصلًا | تعطيل الحاجز في الـworkflow |
| بيانات خاطئة دخلت سجلًّا ملحقًا-فقط | قيد تصحيحي جديد يشرح | حذف السجل أو تعديله |
| سرّ تسرّب إلى كومِت | أبلغ المالك فورًا — يُبطَل المفتاح ويُستبدل من مصدره | الاكتفاء بحذفه في كومِت تالٍ (يبقى في التاريخ) |

---

## 13. ممنوعات مطلقة (بلا استثناء)

1. الدفع إلى `main` بلا أمر «ارفع».
2. `--force` أو إعادة كتابة تاريخ `main`.
3. حذف صفحة أو ميزة أو تغيير رابط قائم.
4. تعديل أو حذف سجل في مجموعة ملحقة-فقط.
5. وضع سرّ حقيقي في الريبو أو خلف بادئة `PUBLIC_`.
6. إضافة CDN أو خطّ خارجي أو أي أصل غير مستضاف ذاتيًّا.
7. تعطيل اختبار أو تخفيف حاجز لتمرير شفرة.
8. إيموجي في الواجهة، أو أحمر لغير التحذير.
9. تلفيق توقيع رسمي أو رقم إشاري.
10. نشر قواعد Firestore بيد غير يد المالك.

---

## 14. تمرين التدريب — رفعتك الأولى، خطوة بخطوة

**السيناريو:** إضافة شاشة لوحة جديدة تكتب في مجموعة Firestore جديدة. هذا التمرين يمرّ بكل بوابة في الدليل.

| الخطوة | الأمر / الفعل | معيار القبول |
|---|---|---|
| 1 | `git pull` + قراءة رأس `SESSION_HANDOFF.md` | تعرف ما لم يُرفع بعد ومن يعمل على ماذا |
| 2 | افتح فرعًا: `git checkout -b codex/تمرين-الرفع` | الفرع ببادئتك |
| 3 | أنشئ `src/pages/dashboard/<اسمها>.astro` داخل `<DashboardLayout>` | الصفحة تُبنى |
| 4 | أضِفها إلى `NAV_GROUPS` في `navCatalog.js` مع `roles` إن كانت حسّاسة | بلا هذه الخطوة يفشل التدقيق (صفحة يتيمة) |
| 5 | ضع المنطق في `src/services/<نطاق>/` **لا** داخل `<script is:inline>` | المنطق قابل للاستيراد والاختبار |
| 6 | اكتب اختبارًا بجواره: `services/<نطاق>/<اسم>.test.js` | `node --test` يلتقطه تلقائيًّا |
| 7 | أضِف قاعدة للمجموعة الجديدة في `firestore.rules` + `npm run test:rules` | بلا قاعدة يفشل التدقيق، وبلا نشر ترتدّ الكتابة |
| 8 | البوابة الخضراء: `npm test && npm run audit && npm run build` | 728+ خضراء · تدقيق نظيف · بناء ناجح |
| 9 | لينت: `npx eslint src` وقارن بما قبل تغييرك | لا مشاكل **جديدة** |
| 10 | مرّ على قائمة الهوية (§6) وقائمة الأمن (§7) بندًا بندًا | لا إيموجي · لا لون مباشر · لا سرّ · لا CDN |
| 11 | تحقّق بصري: `npm run preview` وافتح الشاشة | تعمل على البناء لا على dev |
| 12 | `git status` + `git diff --stat` ثم الكومِت بالصيغة العربية | لا ملف دخيل في التغيير |
| 13 | **اطلب الإذن**، ثم `git push origin HEAD:main` | لا دفع قبل «ارفع» |
| 14 | راقب Actions حتى الاخضرار الثلاثي | Quality Gate → Build → Deploy |
| 15 | افتح الحيّ، `Ctrl+Shift+R`، جرّب بدور غير الأدمن، افحص الكونسول | صفر أخطاء |
| 16 | حدّث `SESSION_HANDOFF.md` وأبلغ المالك بما بقي معلّقًا (نشر القواعد) | البند المعلّق مكتوب صراحةً |

**اجتزت التمرين إذا:** مرّت الرفعة من غير أن يضطرّ المالك لتصحيح شيء بعدها، وكان البند المعلّق عليه (نشر القواعد) مذكورًا قبل أن يسأل عنه.

---

## 15. فخاخ وقعنا فيها فعلًا (لا تكرّرها)

- **`astro dev` يكذب عليك** في جزر React (خلل ترطيب `createRoot`) — افحص على `npm run preview`.
- **كاش Pages عشر دقائق** — لا تعلن فشل النشر قبل تحديث قسري.
- **`global.css` يسحق أيقونات FontAwesome بـ`!important`** — انتبه عند أي نمط عام جديد.
- **`BASE_URL` في Astro 6 قد يأتي بلا شرطة ختامية** — طبّعه قبل التركيب وإلا التصقت الروابط.
- **المسارات العربية تصل مُرمَّزة** من `location.pathname` — `decodeURIComponent` قبل المقارنة.
- **`eslint .` يجرف مجلدات لا تخصّك** (أشجار العمل والمولّدات) فيضخّم العدد — قِس على `src` عند الشك.
- **«نشرتُ القواعد» ليست دليلًا** — المطابقة الحرفية هي الدليل.
- **الصفحات الضخمة ذات المنطق المضمَّن** (`stock-operations` · `assets-inventory` · `erp-workflows`) دَين تقني معروف: لا تُضِف إليها، واستخرج عند التعديل.

---

## 16. البطاقة المرجعية (انسخها والصقها فوق مكتبك)

```
قبل العمل:      git pull  →  اقرأ SESSION_HANDOFF  →  افتح فرع claude/* أو codex/*
البوابة:        npm test  &&  npm run audit  &&  npm run build     (+ npx eslint src)
الهوية:         بلا إيموجي · الأحمر للتحذير · اللون من tokens · 3 طبقات · لا CDN · لا تغيير URL
الأمن:          لا سرّ في الريبو · PUBLIC_ = علنيّ · القواعد ينشرها المالك · الملحق-فقط مقدّس
الكومِت:        feat(نطاق): وصف عربي     +  git status و git diff --stat قبل add
الدفع:          «ارفع» من المالك  →  git push origin HEAD:main
بعده:           راقب Actions  →  Ctrl+Shift+R على الحيّ  →  جرّب بدور  →  حدّث SESSION_HANDOFF
الطوارئ:        git revert فقط — لا force ولا حذف
```

---

## ملحق — الأرقام المرجعية (تُحدَّث عند كل تغيّر جوهري)

| المؤشّر | القيمة | تاريخ القياس |
|---|---|---|
| اختبارات الوحدة | 728 خضراء | 2026-08-07 |
| صفحات البناء | 64 | 2026-08-07 |
| صفحات اللوحة على القرص | 62 (كلها مربوطة) | 2026-08-07 |
| روابط القائمة الداخلية | 58 | 2026-08-07 |
| قواعد `firestore.rules` | 44 | 2026-08-07 |
| الأدوار | 14 | 2026-08-07 |
| الصفحات الحسّاسة المحصورة | 28 | 2026-08-07 |
| خط أساس اللينت (كامل الريبو) | 126 مشكلة (53 خطأ · 73 تحذيرًا) | 2026-08-07 |

> إن اختلف رقمٌ عندك عن هذا الجدول: **قِس ولا تفترض** — ثم حدّث الجدول في كومِت الوثائق.
