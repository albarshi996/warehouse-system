---
/**
 * العمليات المخزنية — شاشةٌ واحدة: امسح فتُعبَّأ (SAP-19 · طلب المالك).
 *
 * تصحيح 2026-08-13: كانت هنا صفحتان (سريعة + كلاسيكيّة) — رفضه المالك:
 * «صفحة فوق صفحة خطأ كارثيّ، نريد منطقًا في التنفيذ». فدُمجتا في شاشةٍ
 * واحدة بمصدر حقيقةٍ واحد: المسح ثلاث خطوات، والجدول يُشتقّ من دفتر
 * العملية الملحق-فقط (فالعمل الجماعيّ حيٌّ بطبيعته)، والكمّيّة الدفتريّة
 * من ماستر الأصناف لا من استيراد شيتٍ ثانٍ، والتصدير من الجدول نفسه.
 * كلّ الحكم في `services/stock/scanFlow.js` الخالص المُختبَر.
 */
import DashboardLayout from '../../layouts/DashboardLayout.astro';
import ScanFlow from '../../components/brandzo-erp/stock/ScanFlow.jsx';
import '../../styles/tokens.css';
import '../../styles/odoo.css';
---

<DashboardLayout title="العمليات المخزنية" showGuide={false} light>
  <div class="text-right" dir="rtl">
    <h1 class="text-lg font-bold text-ink m-0 mb-4">العمليات المخزنية</h1>
    <ScanFlow client:load />
  </div>
</DashboardLayout>
